# v2 新 X 阶段实现验证（2026-09-25）

本轮代码实现 `tapd_ctm_visual_revisit`、schema v2、`X→C→F` 与视觉重访惩罚。未启动远端正式训练。

| 验证 | 结果 |
|---|---|
| `ctm` 环境 `unittest discover -s tests/continual_nav` | 63 项通过；包含重访公式、虚拟时间、终止/重置帧隔离、GroupNorm、PPO 梯度与旧 schema 拒绝。 |
| CUDA 完整 smoke（本地 RTX4060 Laptop） | 18 个阶段、600 训练环境步、8 次 X 联合更新、5 组评估记录，含阶段边界恢复；最终 artifact 可重新加载。显存峰值 allocated 482,951,168 B、reserved 536,870,912 B。产物：`runs/continual_nav/v2_cuda_smoke_release2/`。 |
| RTX5090 配置的 16 槽训练批次形状，本地 RTX4060 Laptop | 缩小预算完整 12 阶段、6,464 训练环境步；X/P 每次 16 槽×50 步，4 个 minibatch。显存峰值 allocated 3,899,497,472 B、reserved 4,324,327,424 B。此探针评估后端设为 serial/2 槽；常规 smoke 已单独覆盖 subprocess 评估。产物：`runs/continual_nav/v2_5090_shape_full_release/`。 |
| W&B offline X 单阶段 | 运行成功，X 的 PPO、SIGReg、奖励、动作/相似度/间隔分布写入 W&B 离线 run 和本地 `metrics.jsonl`。临时 run 已清理。 |
| v2 checkpoint | 最新 CUDA smoke 的视觉 AdamW step 为 8，与累计 X 联合更新一致；最终 checkpoint `finalized=True`、schema 2。 |

正式 RTX5090 配置仍为 4 visits、16 轮 X、每轮 200,000 环境步；配置预算由 `python -m tasks.continual_nav.config --config tasks/continual_nav/configs/rtx5090_32gb.yaml` 校验。旧实例 `workspace.featurize.cn:56762` 在本轮 TCP 探测中无法连接，因此 **尚未在实际 RTX5090 上测量速度或显存，也未启动正式训练**。上述 16 槽显存是本地 RTX4060 的缩小预算实测，不能替代远端实测。
