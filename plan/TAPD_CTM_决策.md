# 决策


| 待决策项      | 决策                                   |
| --------- | ------------------------------------ |
| 图像尺寸      | 例如统一到 `84×84` 或 `112×112`——使用`84×84` |
| 渲染风格      | 保留两种环境原来的颜色、形状                       |
| ResNet 规格 | 使用CTM在2D maze中的规格`resnet34-2`        |
| 时间输入      | 用单帧 RGB，由 CTM 负责历史记忆                 |
| 空间位置编码    | 增加一个旋转位置编码RoPE                       |





| 待决策项                           | 决策                                                |
| ------------------------------ | ------------------------------------------------- |
| $S_{\mathrm{action}}$是否也使用滑动窗口 | **使用**，与 RL 的 $S_{\mathrm{out}}$ 保持一致，但两者有独立衰减参数。 |
| 每个环境步执行几个 tick                 | 两任务先用相同数量。暂定2ticks                                |
| NLM 历史和同步性历史是否同长               | 首轮共用 $M=20$                                       |
| 是否监督中间 tick                    | 首轮只在最后一个 tick 输出动作/value                          |
| 状态何时重置                         | episode 结束及任务切换时重置；正常环境步之间保留                      |
| 跨多少环境步反传                       | 由 rollout/截断反传长度确定，不能与历史窗口 $M$混为一谈。               |


三、知识库保留范围

使用**固定共享 ResNet＋完整双列控制器，ResNet在Task-agnostic阶段训练完成后冻结。**考虑使用LeJEPA中的SIGReg技术。完整Task-Agnostic训练流程细节见："D:\\CodeProject\\TAPD\\plan\\Task-Agnostic：世界模型 + [好奇探索蒸馏.md](http://好奇探索蒸馏.md)"

第四部分在Task-Agnostic训练流程细节里已经回答



五、Maze 改为 RL：奖励与 episode 定义


| 待决策项        | 决策                              |
| ----------- | ------------------------------- |
| 奖励方案        | 若优先对齐原 4Rooms，选第一种              |
| 最大步数        | 先统一为 300                        |
| Maze 起终点    | 沿用数据中的起终点                       |
| Maze 撞墙/越界  | 倾向原地不动、消耗一步                     |
| 任务无关阶段的目标终止 | 到达目标结束                          |
| 超时处理        | 应区分真正终止与时间上限，进行 value bootstrap |




六、训练调度

这部分在Task-Agnostic训练流程细节回答了下面问题：

- **Task-agnostic 是否保证覆盖两个任务？**
- **后续任务顺序是什么？**
- **探索阶段切换任务时是否重置 Active？**

其他问题回答：

- **重置模型时是否同时重置优化器？** 清空优化器历史
- **迁移哪些超参数？** 参考4Rooms的配置拟定一般初始版本



七、循环蒸馏与 EWC

- 蒸馏数据由教师还是学生采样？教师采样。
- 蒸馏序列多长，序列开始是否需要一段历史预热？需要，预热长度与历史窗口长度 $M$一致
- 只蒸馏最终动作分布，还是增加 value/内部表示约束？只保留必要的动作 KL
- KB 的冻结是否包括 BatchNorm 的运行统计？**需要包括**，否则参数不变也可能改变策略
- 严格保持任务无关阶段不利用外在奖励，采用不依赖奖励的策略 Fisher



八、怎样判断整合有效


| 待决策项   | 决策                                             |
| ------ | ---------------------------------------------- |
| 最终评估对象 | KB 独立策略作为知识保持对象，Active 单独用于评估适应速度              |
| 指标     | 成功率、达到目标的步数、学习曲线，以及学完另一任务后的性能下降                |
| 对照组    | 单任务 CTM、顺序 PPO CTM、无任务无关阶段的 P&amp;C            |
| 测试划分   | Maze 用独立测试地图，4Rooms 用独立固定测试 seeds              |
| 随机性    | 训练 seed、任务采样 seed、环境 seed 分开记录；正式比较使用暂定4个 seed |


