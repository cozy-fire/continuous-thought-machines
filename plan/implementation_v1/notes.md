# 编写依据与核验记录

## 用户输入

- ../TAPD_CTM_决策.md
- ../Task-Agnostic：世界模型 + 好奇探索蒸馏.md
- ../Task-agnostic未启用对比项.md
- ../TAPD_CTM_整合设想与待决策项.md（背景；与新决策冲突时不采用旧建议）

## 当前确认

- 新 Task-Agnostic 共有 24 个三阶段轮次；旧的 5 次随机任务抽样已被替代。
- 主流程和未启用项对任务切换时 Active 初始化相冲突；用户明确以主流程为准：每轮与任务切换均随机初始化。
- 世界模型、探索和蒸馏预算已获用户确认；投影头、预测器、SIGReg 与高误差回放细节已补为明确标注的工程初值。
- 本地 CTM ResNet helper 随机初始化且包含 BatchNorm；共享视觉只在世界模型阶段 train，其余阶段 eval。
- CTM RL 现有超时与终止合并，需新协议显式分离；原 RL 模型不含 Attention。
- 本地 TAPD 的 advantage 加权 Fisher 会读取外在奖励，不适用于本次决策，需按策略 log-prob 计算经验 Fisher。
- 未发现 CTM 根目录 environment.yml；依赖文件需按实际文件名查找，不能假设该名称存在。
- 用户已同意W采集100k/更新5000、X300k、C300k，后续P2.5M/C1M/3visits为审阅稿工程初值。
- 用户已选择共享冻结视觉checkpoint的条件对照，不做完整从零视觉对照。
- 本地ResNet实际forward得到(1,128,21,21)，输入(1,3,84,84)；torch=2.13.0+cu126、gymnasium=1.3.0、minigrid=3.1.0。
- SIGReg核对本地LeJEPA MINIMAL.md及官方galilai-group/lejepa；RoPE核对naver-ai/rope-vit，单Query中心坐标由本计划明确为工程约定。

## 文档原则

以明确张量形状、更新所有权、事件时序、计数单位、重置边界和可执行验收条件取代“适当”“类似”“视情况”。不把视觉参数冻结误写成世界模型更新期间 KB 整体函数不变。未经实际训练不声称方案已收敛或实现成功。

## 独立读者检查

两名只读审阅者分别检查跨模块契约和用户决策覆盖。已补齐：C的逐rollout更新频率、Fisher固定窗口及计分掩码、单列PPO/保存恢复接口、GAE原值与归一化副本、init/W.collect恢复点、smoke动态batch、世界模型版本提交时点。已收紧条件基线归因表述，列出共享成本和新增成本；不增加用户未要求的消融。

原始决策文件未修改；本文件包是审阅稿，不代表用户已批准新增工程默认。

最终结构检查：6个Markdown文件UTF-8可读，14个本地链接有效，代码围栏闭合；预算算术与02正文一致。检查覆盖主方法每seed37,922,880、全方法每seed88,947,456、四seed全方法355,789,824及smoke760，均不含评估与中断重跑。此项为文档验证，未执行拟建实现的测试。
