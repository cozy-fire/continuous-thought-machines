# 首轮实现计划文档编写进度

本次任务只生成供用户审阅的实现文档，不修改模型、训练代码、依赖或数据，不启动训练。

## 工作阶段

- [x] 读取用户决策、Task-Agnostic 主流程及未启用对比项。
- [x] 识别主流程与未启用项的 Active 初始化冲突，发出异步澄清。
- [x] 核验 ResNet、RoPE、SIGReg、循环 PPO 与环境接口的实现依据。
- [x] 完成主计划、模型与数据契约、训练协议、分工与验收四份文档。
- [x] 使用独立读者审查，修正文档矛盾并核验交叉引用。
- [x] 向用户交付审阅入口。

## 已锁定约束

- 84×84 RGB、原生渲染风格、Maze 全图与 4Rooms 局部视野、无 task ID 输入、单帧输入。
- resnet34-2；新增空间 RoPE；CTM 每步 2 ticks；预激活与同步窗口均 M=20。
- 共享 ResNet；Task-Agnostic 分阶段更新视觉；结束后冻结视觉参数与运行统计。
- Task-Agnostic 固定 Maze→4Rooms，6 visits，每次任务访问执行 2 轮世界模型→探索→蒸馏。
- PPO-clip；仅动作 KL；独立循环状态；无外在奖励的策略 Fisher；4 个训练 seed。

## 澄清与补充原则

用户原文中没有给定的数值和接口必须显式标记为“本计划工程默认”，不得伪称已由用户决定。主流程与未启用对比项的冲突以用户本次澄清为准。未收到答复时保留明确的暂定值，交付状态为审阅稿。

## 当前状态

四份审阅稿已完成并交付。两名独立读者的必要修订已纳入；14个本地链接、代码围栏闭合及主方法/全部条件基线/smoke预算算术检查通过。用户已确认Active随机初始化、预算初值、共享冻结视觉的条件基线；其余工程默认等待本次审阅。本轮没有修改训练代码或启动训练。
